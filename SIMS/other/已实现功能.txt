涉及到修改数据库内容的部分做了输入验证及错误提示，保证输入内容可进行数据库更新

将连接数据库时用到的驱动名、账号等信息单独放到content目录下database.properties配置文件下，便于直接从外部修改

登录界面：
    访问数据库中账号表，并进行账号验证
    内置默认管理员账号，账号密码都是admin，无需连接数据库验证
    依据用户类型进行界面跳转

主界面：
    注销按钮（返回登陆界面）
    修改密码按钮
    欢迎语句和当前时间（本打算设为学年＋学期＋周，但判断是第几周不太好实现，改为显示当前年月日）

学生主界面：
    个人信息页面：
        获取数据库信息并显示
        地址、电话编辑：
            点击编辑按钮进行布局切换
            输入框简单检错：不能为空、新旧不能相同、不能过长
            响应回车，触发确认按钮
    成绩查询页面：
        普通课程查询页面：
            所有成绩查询功能
            筛选功能
            进入时默认显示所有成绩
        学业完成情况页面：
            计算已结算成绩课程的总学分

管理员主界面：
    学生管理界面：
        进入时默认显示所有学生信息
        搜索功能（支持部分搜索，即可只输入部分信息）
        下拉框内容从数据库中已存在信息导入
        学生信息的添加、修改、删除功能（添加新学生时会自动创建对应账号，删除时会同时删掉score表对应数据）
    教师管理界面
    课程管理界面

修改密码界面

待优化：
计算绩点
course表查询方法优化
界面美化